import { useState } from 'react';
import { BookOpen,FileText,Headphones,Play } from 'lucide-react';
import { useI18n } from '@/lib/i18n';
import { useRouter, parseQuery } from '@/lib/router';
import { comprehensiveSpecialties } from '@/lib/comprehensiveSpecialties';
import { supabase } from '@/lib/supabase';
import { virtualArticlesForSpecialty,virtualVideosForSpecialty,virtualAudioForSpecialty,virtualLibraryForSpecialty,specialtyCatalog } from '@/lib/catalog';
import { useEffect } from 'react';
export default function MediaHubPage(){const {lang,dir}=useI18n(); const [db,setDb]=useState<any>({articles:[],videos:[],audio:[],library:[]});
 useEffect(()=>{Promise.all([supabase.from('articles').select('*, specialty(*), doctor(*)').limit(300),supabase.from('doctor_videos').select('*, specialty(*), doctor(*)').limit(300),supabase.from('doctor_audio').select('*, specialty(*), doctor(*)').limit(300),supabase.from('specialty_library_items').select('*, specialty(*)').limit(300)]).then(([a,v,au,l])=>setDb({articles:a.data||[],videos:v.data||[],audio:au.data||[],library:l.data||[]})).catch(()=>setDb({articles:[],videos:[],audio:[],library:[]}))},[lang]); const c:any={ar:{title:'المحتوى الطبي والمكتبة',sub:'المقالات والفيديو والتسجيل الصوتي والكتب حسب اللغة المختارة.',spec:'اختار التخصص',articles:'المقالات',videos:'الفيديوهات',audio:'التسجيل الصوتي',library:'المكتبة',ai:'{c.ai}'},en:{title:'Medical Content & Library',sub:'Articles, videos, audio and books in the selected language.',spec:'Choose specialty',articles:'Articles',videos:'Videos',audio:'Audio',library:'Library',ai:'AI-generated educational draft — editorial review required'},ru:{title:'Медицинский контент и библиотека',sub:'Статьи, видео, аудио и книги на выбранном языке.',spec:'Выберите специальность',articles:'Статьи',videos:'Видео',audio:'Аудио',library:'Библиотека',ai:'Материал создан ИИ — требуется редакторская проверка'},de:{title:'Medizinische Inhalte & Bibliothek',sub:'Artikel, Videos, Audio und Bücher in der ausgewählten Sprache.',spec:'Fachgebiet wählen',articles:'Artikel',videos:'Videos',audio:'Audio',library:'Bibliothek',ai:'KI-generierter Entwurf — redaktionelle Prüfung erforderlich'}}[lang]||{title:'Medical Content & Library',sub:'Content in the selected language.',spec:'Choose specialty',articles:'Articles',videos:'Videos',audio:'Audio',library:'Library',ai:'AI-generated draft'};const {path,navigate}=useRouter();const query=parseQuery(path);const routeTab=path.split('?')[0].split('/')[1];const initialTab=['articles','videos','audio','library'].includes(query.type||'')?(query.type as string):(['articles','videos','audio','library'].includes(routeTab)?routeTab:'articles');const [tab,setTab]=useState(initialTab);const [slug,setSlug]=useState(query.specialty||comprehensiveSpecialties[0].slug);const realArticles=db.articles.filter((x:any)=>x.translations?.[lang]&&x.specialty?.slug===slug).map((x:any)=>({...x,title:x.translations[lang].title||x.title,excerpt:x.translations[lang].excerpt||x.excerpt,body:x.translations[lang].body||x.body,content_language:lang})); const articles=[...realArticles,...virtualArticlesForSpecialty(slug,lang,5)],videos=[...db.videos.filter((x:any)=>x.translations?.[lang]&&x.specialty?.slug===slug).map((x:any)=>({...x,title:x.translations[lang].title||x.title,description:x.translations[lang].description||x.description})),...(['ar','ru','en','ka','hy','uz'].includes(lang)?virtualVideosForSpecialty(slug,lang,2):[])],audio=[...db.audio.filter((x:any)=>x.translations?.[lang]&&x.specialty?.slug===slug).map((x:any)=>({...x,title:x.translations[lang].title||x.title,description:x.translations[lang].description||x.description})),...virtualAudioForSpecialty(slug,lang,3)],books=[...db.library.filter((x:any)=>x.translations?.[lang]&&x.specialty?.slug===slug).map((x:any)=>({...x,title:x.translations[lang].title||x.title,description:x.translations[lang].description||x.description})),...virtualLibraryForSpecialty(slug,lang,3)];const tabs:any=[['articles',FileText,c.articles],['videos',Play,c.videos],['audio',Headphones,c.audio],['library',BookOpen,c.library]];return <div className="min-h-screen bg-gray-50 pt-24 pb-16" dir={dir}><div className="mx-auto max-w-7xl px-4"><div className="rounded-3xl bg-gradient-to-br from-teal-700 to-cyan-600 p-8 text-white"><h1 className="text-3xl font-extrabold">{c.title}</h1><p className="mt-2">{c.sub}</p></div><div className="mt-5 flex flex-wrap gap-2 rounded-2xl bg-white p-3 border">{tabs.map((x:any)=>{const Icon=x[1];return <button key={x[0]} onClick={()=>setTab(x[0])} className={'flex items-center gap-2 rounded-xl px-5 py-3 font-bold '+(tab===x[0]?'bg-teal-600 text-white':'bg-gray-50 text-gray-700')}><Icon className="h-4 w-4"/>{x[2]}</button>})}</div><div className="my-5 rounded-2xl bg-white p-4 border"><label className="block text-sm font-bold mb-2">{c.spec}</label><select value={slug} onChange={e=>setSlug(e.target.value)} className="w-full rounded-xl border px-4 py-3">{specialtyCatalog(lang).map(x=><option key={x.slug} value={x.slug}>{x.name}</option>)}</select></div>{tab==='articles'&&<div className="grid gap-5 md:grid-cols-3">{articles.map(a=><article key={a.id} onClick={()=>navigate('/articles/'+a.id)} className="cursor-pointer rounded-2xl bg-white border p-5 hover:shadow-lg transition"><FileText className="text-teal-600 mb-3"/><h3 className="font-bold">{a.title}</h3><p className="mt-2 text-sm text-gray-500">{a.excerpt}</p><p className="mt-3 text-xs text-teal-600">مُنشأ بالذكاء الاصطناعي — يحتاج مراجعة تحريرية</p></article>)}</div>}{tab==='videos'&&<div className="grid gap-5 md:grid-cols-2">{videos.map(v=><div key={v.id} className="rounded-2xl bg-white border p-5"><video src={v.video_url} controls className="w-full rounded-xl bg-black"/><h3 className="mt-3 font-bold">{v.title}</h3></div>)}</div>}{tab==='audio'&&<div className="grid gap-4">{audio.map(a=><div key={a.id} className="rounded-2xl bg-white border p-5 flex items-center gap-4"><Headphones className="h-8 w-8 text-teal-600"/><div><h3 className="font-bold">{a.title}</h3><audio controls src={a.audio_url} className="mt-2"/></div></div>)}</div>}{tab==='library'&&<div className="grid gap-5 md:grid-cols-2">{books.map(b=><div key={b.id} className="rounded-2xl bg-white border p-6"><BookOpen className="text-teal-600 mb-3"/><h3 className="font-bold">{b.title}</h3><p className="text-sm text-gray-500 mt-2">{b.description}</p></div>)}</div>}</div></div>}
