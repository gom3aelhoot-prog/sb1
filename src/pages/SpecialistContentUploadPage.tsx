import {useEffect,useState} from 'react';import {Upload,FileText,Video,Headphones,BookOpen,Clock,ShieldCheck} from 'lucide-react';import {supabase} from '@/lib/supabase';import {useI18n} from '@/lib/i18n';import {specialtyCatalog} from '@/lib/catalog';
export default function SpecialistContentUploadPage(){const {lang,dir}=useI18n();const [type,setType]=useState('article');const [title,setTitle]=useState('');const [description,setDescription]=useState('');const [specialty,setSpecialty]=useState(specialtyCatalog(lang)[0]?.slug||'');const [price,setPrice]=useState('0');const [file,setFile]=useState<File|null>(null);const [cover,setCover]=useState<File|null>(null);const [busy,setBusy]=useState(false);const [msg,setMsg]=useState('');const [userId,setUserId]=useState('');
useEffect(()=>{supabase.auth.getUser().then((r:any)=>setUserId(r.data?.user?.id||''))},[]);
const submit=async(e:any)=>{e.preventDefault();setBusy(true);setMsg('');try{let filePath='',coverPath='';if(file&&supabase.storage){filePath=`${userId||'anonymous'}/${Date.now()}-${file.name}`;const u=await supabase.storage.from('specialist-content').upload(filePath,file,{upsert:false});if(u.error)throw u.error;if(cover){coverPath=`${userId||'anonymous'}/covers-${Date.now()}-${cover.name}`;const cu=await supabase.storage.from('specialist-content').upload(coverPath,cover,{upsert:false});if(cu.error)throw cu.error;}}const spec=specialtyCatalog(lang).find(x=>x.slug===specialty);const payload={specialist_id:userId||null,content_type:type,title,description,specialty_id:spec?.id||null,language:lang,price:Number(price)||0,platform_share:50,specialist_share:50,file_path:filePath||null,cover_path:coverPath||null,status:'pending'};const {error}=await supabase.from('content_submissions').insert(payload);if(error)throw error;setMsg('تم إرسال المحتوى للمراجعة. لن يظهر للمستخدمين قبل موافقة إدارة SB1.');setTitle('');setDescription('');setFile(null);setCover(null);}catch(err:any){setMsg(err?.message||'تعذر إرسال المحتوى');}finally{setBusy(false)}};const Icon=type==='article'?FileText:type==='video'?Video:type==='audio'?Headphones:BookOpen;return <div dir={dir} className="min-h-screen bg-gray-50 pt-24 pb-16"><div className="mx-auto max-w-3xl px-4"><div className="rounded-3xl bg-white border p-7 shadow-sm"><div className="flex items-center gap-3"><div className="rounded-2xl bg-teal-50 p-3"><Icon className="h-7 w-7 text-teal-700"/></div><div><h1 className="text-2xl font-extrabold">رفع محتوى للأخصائي</h1><p className="text-sm text-gray-500">مجاني أو مدفوع — 50% للموقع و50% للأخصائي.</p></div></div><div className="mt-5 rounded-2xl bg-amber-50 p-4 text-sm text-amber-800"><Clock className="inline h-4 w-4 me-1"/> المحتوى يدخل حالة «قيد المراجعة» ولا يظهر للجمهور قبل موافقة الإدارة.</div><form onSubmit={submit} className="mt-6 space-y-4"><div className="grid grid-cols-2 md:grid-cols-4 gap-2">{[['article','مقال'],['video','فيديو'],['audio','تسجيل صوتي'],['book','كتاب']].map(x=><button type="button" onClick={()=>setType(x[0])} className={`rounded-xl px-3 py-3 border font-bold ${type===x[0]?'bg-teal-600 text-white':'bg-white'}`} key={x[0]}>{x[1]}</button>)}</div><input required value={title} onChange={e=>setTitle(e.target.value)} className="input-field w-full" placeholder="عنوان المحتوى"/><textarea value={description} onChange={e=>setDescription(e.target.value)} className="input-field w-full min-h-28" placeholder="الوصف"/><select value={specialty} onChange={e=>setSpecialty(e.target.value)} className="input-field w-full">{specialtyCatalog(lang).map(s=><option key={s.slug} value={s.slug}>{s.name}</option>)}</select><div><label className="text-sm font-bold">السعر (0 = مجاني)</label><input type="number" min="0" step="0.01" value={price} onChange={e=>setPrice(e.target.value)} className="input-field w-full mt-1"/></div><div><label className="text-sm font-bold">ملف المحتوى</label><input required type="file" onChange={e=>setFile(e.target.files?.[0]||null)} className="mt-2 w-full"/></div><div><label className="text-sm font-bold">صورة الغلاف</label><input type="file" accept="image/*" onChange={e=>setCover(e.target.files?.[0]||null)} className="mt-2 w-full"/></div><div className="rounded-xl bg-gray-50 p-4 text-sm"><ShieldCheck className="inline h-4 w-4 text-teal-600 me-1"/> عند البيع: 50% للموقع و50% للأخصائي بعد اعتماد المحتوى وإتمام العملية.</div>{msg&&<div className="rounded-xl bg-teal-50 text-teal-800 p-4">{msg}</div>}<button disabled={busy} className="btn-primary w-full flex items-center justify-center gap-2"><Upload className="h-5 w-5"/>{busy?'جارٍ الإرسال…':'إرسال للمراجعة'}</button></form></div></div></div>}